services:
  - type: web
    name: ai-trading-2.0
    runtime: node
    startCommand: "node server.js"
    buildCommand: "npm install"
    port: 3000
    envVars:
      - key: NODE_ENV
        value: production